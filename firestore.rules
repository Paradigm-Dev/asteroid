service cloud.firestore {
  match /databases/{database}/documents {
  	// match /{document=**} {
  	//   allow read, write
  	// }
  	
    match /{document=**} {
      allow read, write: if request.auth.uid != null;
    }
    match /paradigm/{doc=**} {
      allow read;
      allow write: if request.auth.uid != null
    }
    match /users/{user=**} {
      allow read, write;
    }
    match /electorate/{doc=**} {
    	allow read
    }
  }
}